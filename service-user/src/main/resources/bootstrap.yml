
spring:
  application:
      name: service-user
  config:
    import: optional:configserver:http://localhost:8081
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
      username: root
      password: s3cr3t
